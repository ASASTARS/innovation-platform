<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abajin.innovation.mapper.ProjectApplicationMemberMapper">
    <resultMap id="BaseResultMap" type="com.abajin.innovation.entity.ProjectApplicationMember">
        <id column="id" property="id"/>
        <result column="application_form_id" property="applicationFormId"/>
        <result column="student_id" property="studentId"/>
        <result column="name" property="name"/>
        <result column="major" property="major"/>
        <result column="main_work" property="mainWork"/>
        <result column="signature" property="signature"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM project_application_member WHERE id = #{id}
    </select>

    <select id="selectByApplicationFormId" resultMap="BaseResultMap">
        SELECT * FROM project_application_member 
        WHERE application_form_id = #{applicationFormId} 
        ORDER BY sort_order ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO project_application_member (
            application_form_id, student_id, name, major, main_work, signature, sort_order, create_time
        ) VALUES (
            #{applicationFormId}, #{studentId}, #{name}, #{major}, #{mainWork}, #{signature}, #{sortOrder}, #{createTime}
        )
    </insert>

    <update id="update">
        UPDATE project_application_member SET
            student_id = #{studentId},
            name = #{name},
            major = #{major},
            main_work = #{mainWork},
            signature = #{signature},
            sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM project_application_member WHERE id = #{id}
    </delete>

    <delete id="deleteByApplicationFormId">
        DELETE FROM project_application_member WHERE application_form_id = #{applicationFormId}
    </delete>
</mapper>
